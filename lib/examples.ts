/**
 * 프로젝트 유형별 few-shot 모범 예시 — 순수 데이터 파일.
 *
 * ⚠️ 이 파일은 "데이터만" 담는다. 유형 판별/프롬프트 조립 로직은 lib/prompts.ts에 있음.
 * 예시를 교체할 때는 EXAMPLES 상수의 문자열만 갈아끼우면 된다 (구조 유지, 각 문서 800자 이상).
 */

export type ProjectType = "web" | "backend-infra" | "ai-data";

/** 문서 3종이 한 세트 — 유형마다 2세트씩 */
export interface ExampleSet {
  readme: string;
  blog: string;
  qa: string;
}

export interface ProjectTypeExamples {
  /** 프롬프트에 표기되는 유형 이름 */
  label: string;
  /** README/블로그/Q&A 각 2개 = 세트 2개 */
  sets: [ExampleSet, ExampleSet];
}

/* ─────────────────────────────────────────────
   유형별 정식 예시 (web: 어디팔지·런크루 / backend: 썸네일리·랭킹워크 / ai: 미닛츠·시세어때)
───────────────────────────────────────────── */

const WEB_SET_1: ExampleSet = {
  readme: `# 어디팔지
> 중고나라·번개장터·당근의 시세를 한 번에 비교하고, 원하는 가격이 되면 알림을 받는 서비스
## 📌 프로젝트 개요
중고 물품을 팔 때마다 플랫폼 3곳을 오가며 시세를 검색하는 게 번거로워 직접 만들었습니다.
키워드를 등록하면 매일 아침 각 플랫폼의 최저가·평균가를 수집해 대시보드로 보여주고,
목표 가격 도달 시 이메일 알림을 발송합니다.
- **개발 기간**: 2025.11 ~ 2026.01 (8주, 1인 개발)
- **배포 주소**: https://eodipalji.vercel.app
## 🛠 기술 스택
| 분류 | 기술 | 선택 이유 |
| --- | --- | --- |
| Frontend | Next.js 14 (App Router), TypeScript, Tailwind CSS | SSR로 검색 결과 초기 로딩 최적화 |
| Backend / DB | Supabase (PostgreSQL, Auth, Edge Functions) | 인증·DB·스케줄러를 한 서비스로 해결해 1인 개발 속도 확보 |
| 수집 | Playwright, Vercel Cron | 동적 렌더링 페이지 대응 |
| 배포 | Vercel | PR 프리뷰 배포로 배포 리스크 최소화 |
## 🏗 아키텍처
\`\`\`
[Vercel Cron (매일 06:00)]
        │ 트리거
        ▼
[수집 워커 (Playwright)] ──▶ [Supabase PostgreSQL]
                                     │
[Next.js (SSR 대시보드)] ◀───────────┤
[알림 발송 (Edge Function + Resend)] ◀┘ 목표가 도달 시
\`\`\`
## ✨ 주요 기능
- 키워드 기반 시세 수집: 플랫폼 3곳의 매물 제목·가격 정규화 후 저장
- 시세 대시보드: 7일/30일 가격 추이 차트, 최저가 매물 바로가기
- 가격 알림: 목표가 도달 시 이메일 발송 (중복 발송 방지 로직 포함)
- 소셜 로그인: Supabase Auth 기반 구글 로그인
## 🔧 트러블슈팅
### 수집 성공률 62% → 96% 개선
**문제**: 배포 후 일주일간 수집 로그를 확인하니 특정 플랫폼의 수집 성공률이 62%에
그쳤습니다. 원인은 봇 차단이 아니라 매물 제목의 표기 불일치("아이폰15" / "iPhone 15" /
"아이폰 15프로")로 동일 상품이 서로 다른 상품으로 분류되는 것이었습니다.
**해결**: 정규식 기반 전처리(공백·특수문자 정규화, 한/영 모델명 매핑 테이블)를 거친 뒤
매칭하도록 수집 파이프라인을 수정했습니다. 매핑 테이블은 수집 실패 로그에서 미매칭
케이스를 주 1회 추출해 점진적으로 보강했습니다.
**결과**: 수집 성공률 96%, 시세 데이터의 이상치(중복 상품 오분류)도 함께 감소했습니다.
### Vercel Cron 콜드스타트로 인한 수집 타임아웃
**문제**: Playwright 실행이 서버리스 함수의 10초 제한을 초과해 수집이 중단됐습니다.
**해결**: 플랫폼별 수집을 하나의 함수에서 순차 실행하던 구조를, 플랫폼별 함수로 분리하고
Cron이 각각 병렬 트리거하도록 변경했습니다. 함수당 실행 시간이 1/3로 줄어 제한 안에
안정적으로 들어왔습니다.
## 🚀 실행 방법
\`\`\`bash
git clone https://github.com/username/eodipalji
cd eodipalji
cp .env.example .env.local  # Supabase 키 설정
npm install && npm run dev
\`\`\`
`,
  blog: `# 중고거래가 귀찮아서 만든 시세 알림 서비스, 8주 개발 회고
안녕하세요! 오늘은 사이드 프로젝트 "어디팔지"를 만들며 겪은 8주간의 기록을
공유하려고 합니다. 결론부터 말하면 — 크롤링은 만드는 것보다 유지하는 게 10배 어렵고,
저는 그걸 배포 3일 만에 몸으로 배웠습니다.
## 시작은 아이폰 한 대였습니다
쓰던 아이폰을 팔려고 중고나라, 번개장터, 당근을 번갈아 검색하는데 문득 현타가 왔습니다.
"같은 검색을 세 번 하는 걸 왜 사람이 하고 있지?" 그날 밤 노션에 기능 목록을 적기 시작했고,
그게 이 프로젝트의 시작이었습니다.
## 기술 선택: 1인 개발은 "덜어내는 싸움"
처음 설계는 거창했습니다. NestJS 백엔드에 Redis 캐시, 알림용 메시지 큐까지.
그런데 그림을 그려놓고 보니 이걸 8주 안에 혼자 만들 수 있는가에 대한 답이 "아니오"였습니다.
그래서 기준을 바꿨습니다. **"이 기술이 없으면 서비스가 안 되는가?"**
이 질문에 아니오면 전부 뺐습니다. 그 결과가 Next.js + Supabase 조합입니다.
인증, DB, 스케줄 함수까지 Supabase 하나로 해결되니 백엔드 코드가 극적으로 줄었고,
아낀 시간을 전부 수집 파이프라인 품질에 쓸 수 있었습니다.
## 배포 3일 만에 찾아온 위기
배포하고 뿌듯해하던 3일째 아침, 대시보드의 시세 차트가 이상했습니다.
아이폰 15 평균가가 하루 만에 20만 원이 떨어져 있던 겁니다. 폭락장인가? 아니었습니다.
수집 로그를 까보니 성공률이 62%였고, "아이폰15"와 "iPhone 15"를 다른 상품으로
취급하고 있었습니다. 누락된 매물이 많으니 평균이 요동친 거죠.
이때 처음으로 "돌아가는 코드"와 "믿을 수 있는 데이터"는 완전히 다른 문제라는 걸
실감했습니다.
해결은 화려하지 않았습니다. 정규화 전처리 + 모델명 매핑 테이블. 그리고 매주 일요일마다
미매칭 로그를 보며 테이블을 보강하는 지루한 운영 루틴. 하지만 성공률이 96%까지
올라오면서 차트가 비로소 "시세"라고 부를 만한 모양이 됐습니다.
## 만들고 나서 배운 것
- **기능을 빼는 결정이 넣는 결정보다 어렵고, 더 중요했습니다.** 메시지 큐를 뺀 덕에 출시했습니다.
- **데이터 파이프라인은 배포가 시작입니다.** 수집 로그 모니터링을 처음부터 설계에 넣었어야 했습니다.
- 다음 목표는 이메일 알림을 카카오 알림톡으로 바꾸는 것입니다. 오픈율이 다르더라고요.
8주 전의 저처럼 "이거 만들어볼까?" 망설이는 분이 있다면 — 일단 배포까지 가보세요.
진짜 공부는 배포 다음 날부터 시작됩니다. 읽어주셔서 감사합니다! 🙌
`,
  qa: `# 어디팔지 — 면접 예상 질문 & 모범 답안
## Q1. 백엔드 프레임워크 대신 Supabase(BaaS)를 선택한 이유는 무엇인가요? 한계는 없었나요?
**모범 답안**: 8주 1인 개발이라는 제약에서 인증·DB·스케줄러를 직접 구축하면 핵심 가치인
수집 파이프라인에 쓸 시간이 부족하다고 판단했습니다(상황/과제). Supabase로 공통 기능을
해결하고 아낀 시간을 데이터 정합성 개선에 투자했습니다(행동). 다만 Edge Function의
실행 시간 제한과 복잡한 쿼리 튜닝의 제약을 겪었고, 트래픽이 커지면 수집 워커부터
별도 서버로 분리하는 마이그레이션 경로를 염두에 두고 있습니다(결과/한계 인지).
## Q2. 수집 성공률을 62%에서 96%로 올렸다고 했는데, 그 수치는 어떻게 측정했나요?
**모범 답안**: 수집 시도 대비 정상 파싱·매칭된 매물 비율로 정의했고, 수집 워커가 매 실행마다
시도/성공/실패 사유를 로그 테이블에 기록하도록 만들어 측정했습니다. 개선 전후 동일 키워드
셋으로 1주일씩 비교해 수치를 검증했습니다. 측정 체계를 먼저 만든 덕분에 개선 효과를
객관적으로 확인할 수 있었습니다.
## Q3. 크롤링 대상 사이트가 구조를 바꾸면 서비스가 멈출 텐데, 어떻게 대응하시겠습니까?
**모범 답안**: 실제로 가장 큰 운영 리스크라고 생각합니다. 현재는 파싱 실패율이 임계치를
넘으면 로그로 감지하는 수준이고, 개선 방향으로는 ① 셀렉터를 코드에서 분리해 배포 없이
수정 가능하게 하고 ② 공식 API가 제공되는 플랫폼은 API로 전환하는 것을 계획하고 있습니다.
근본적으로는 크롤링 의존도를 낮추는 방향이 맞다고 생각합니다.
## Q4. 가격 알림의 중복 발송은 어떻게 방지했나요?
**모범 답안**: 알림 조건 충족 시 바로 발송하지 않고, 알림 이력 테이블에서 동일 키워드·동일
가격 구간의 발송 이력을 확인한 뒤 24시간 내 이력이 없을 때만 발송하도록 했습니다.
발송과 이력 기록을 하나의 트랜잭션으로 묶어 발송 실패 시 이력만 남는 불일치도 방지했습니다.
## Q5. 이 프로젝트를 다시 만든다면 무엇을 바꾸시겠습니까?
**모범 답안**: 수집 파이프라인의 관측성(observability)을 첫 설계에 포함하겠습니다. 이번에는
문제가 터진 후에야 로그 체계를 만들어서 원인 파악에 이틀이 걸렸는데, 처음부터 성공률
대시보드가 있었다면 반나절이면 충분했을 겁니다. 기능 개발보다 운영 가시성이 먼저라는 게
이 프로젝트의 가장 큰 교훈입니다.
`,
};

const WEB_SET_2: ExampleSet = {
  readme: `# 런크루
> 우리 동네에서 함께 뛸 러닝 메이트를 찾고, 정기 모임을 관리하는 웹 서비스
## 📌 프로젝트 개요
러닝 인구는 늘었지만 혼자 뛰다 그만두는 사람이 많다는 점에 착안해, 지역 기반으로
크루를 만들고 참여를 관리하는 서비스를 팀 프로젝트(3인)로 개발했습니다.
저는 **백엔드 전반과 배포 인프라**를 담당했습니다.
- **개발 기간**: 2025.09 ~ 2025.12 (12주, 3인 팀 / 본인 역할: 백엔드·인프라)
- **주요 성과**: 이미지 업로드 평균 응답 4.2초 → 0.9초 개선
## 🛠 기술 스택
| 분류 | 기술 | 선택 이유 |
| --- | --- | --- |
| Frontend | React, TypeScript | 팀원 담당 |
| Backend | Node.js (Express), MySQL | 팀 전원이 학습 곡선 없이 기여 가능한 스택 |
| Infra | AWS EC2, S3, Nginx, GitHub Actions | 프리티어 내 운영 + CI/CD 자동화 |
## 🏗 아키텍처
\`\`\`
[React (S3 정적 호스팅)]
        │ HTTPS
        ▼
[Nginx (리버스 프록시)] ──▶ [Express API (EC2, PM2)] ──▶ [MySQL (EC2)]
        │                                  │
        └──────── 이미지 직접 업로드 ────▶ [S3 (Presigned URL)]
\`\`\`
## ✨ 주요 기능
- 위치 기반 크루 탐색: 행정동 단위 필터 + 페이스(km당 시간) 조건 검색
- 크루 관리: 정기 모임 일정 생성, 참석 투표, 출석률 통계
- 러닝 인증: 사진 업로드 기반 인증 피드 (S3 Presigned URL 직접 업로드)
- CI/CD: main 브랜치 push 시 GitHub Actions로 테스트 → 빌드 → EC2 배포 자동화
## 🔧 트러블슈팅
### 이미지 업로드 응답 4.2초 → 0.9초
**문제**: 러닝 인증 사진(평균 3~4MB)이 Express 서버를 경유해 S3로 올라가는 구조라
업로드 응답이 평균 4.2초였고, 동시 업로드 시 EC2(t2.micro) 메모리가 출렁이며
서버가 두 차례 다운됐습니다.
**해결**: 클라이언트가 서버에서 Presigned URL만 발급받아 S3에 직접 업로드하도록
구조를 변경했습니다. 서버는 URL 발급과 업로드 완료 검증만 담당하게 되어 메모리
부담이 사라졌고, 프론트 담당 팀원과 협의해 업로드 전 클라이언트 측 이미지 리사이징도
함께 적용했습니다.
**결과**: 평균 응답 0.9초, 업로드 관련 서버 다운 재발 0회.
### 배포할 때마다 발생하던 30초 다운타임 제거
**문제**: GitHub Actions 배포 스크립트가 프로세스를 종료 후 재시작하는 방식이라
배포마다 약 30초의 다운타임이 발생했습니다.
**해결**: PM2 cluster 모드 + reload 방식으로 전환해 프로세스를 순차 재시작하도록
변경하고, Nginx 헬스체크 경로를 추가해 준비되지 않은 프로세스로 트래픽이 가지 않게
했습니다. 이후 배포 중에도 응답 실패가 발생하지 않았습니다.
## 🚀 실행 방법
\`\`\`bash
git clone https://github.com/username/runcrew
cd runcrew/server
cp .env.example .env  # DB, AWS 키 설정
npm install && npm run dev
\`\`\`
`,
  blog: `# 팀 프로젝트에서 서버를 두 번 죽이고 배운 것들 — 런크루 백엔드 회고
안녕하세요! 3인 팀 프로젝트 "런크루"에서 백엔드와 인프라를 맡아 12주를 보냈습니다.
이 글은 성공담이라기보다는, 제가 서버를 두 번 죽이고 나서야 이해하게 된 것들에 대한
기록입니다.
## "백엔드 할 사람?" 에서 시작된 12주
팀 결성 첫날, 셋 다 프론트를 하고 싶어 했습니다. 가위바위보에서 진 제가 백엔드를
맡았는데 — 지금 돌아보면 그 가위바위보가 제 커리어 방향을 바꿨습니다.
스택은 화려한 걸 포기하고 Express + MySQL로 정했습니다. 이유는 단순합니다.
셋 다 아는 스택이어야 제가 막혔을 때 팀원이 코드를 읽고 도와줄 수 있으니까요.
기술 선택 기준이 "내가 배우고 싶은 것"에서 "팀이 굴릴 수 있는 것"으로 바뀐 첫 경험이었습니다.
## 첫 번째 서버 다운: 시연 5분 전
중간 발표 시연 5분 전, 팀원들이 동시에 인증 사진을 올리며 테스트하던 중 서버가
죽었습니다. t2.micro의 1GB 메모리에 4MB짜리 사진 여러 장이 동시에 버퍼로 올라오니
당연한 결과였는데, 당시의 저는 그게 당연한지 몰랐습니다.
급한 대로 재부팅으로 시연은 넘겼지만, 문제를 알면서 방치한 죗값은 2주 뒤
두 번째 다운으로 돌아왔습니다. 이번엔 실사용 테스트 중이었고, 도망칠 곳이 없었습니다.
## 서버가 일을 안 하게 만들기
해결의 실마리는 발상의 전환이었습니다. "서버가 이미지를 더 잘 처리하게 만들자"가
아니라 **"서버가 이미지를 아예 만지지 않게 하자"**.
S3 Presigned URL 방식으로 바꾸면서 서버의 역할은 "업로드 허가증 발급"으로 줄었습니다.
무거운 파일은 클라이언트가 S3에 직접 올리고요. 응답 시간은 4.2초에서 0.9초가 됐고,
그 뒤로 서버는 한 번도 죽지 않았습니다.
이 경험이 좋았던 건, 아키텍처 다이어그램에서 화살표 하나 옮기는 게 코드 백 줄보다
큰 개선을 만들 수 있다는 걸 처음 체감했기 때문입니다.
## 배운 것 세 가지
- **인프라 문제는 코드가 아니라 구조로 풉니다.** 두 번의 다운이 가르쳐준 것.
- **팀 프로젝트의 기술 선택 기준은 "팀의 최대공약수"입니다.** 혼자면 못 배웠을 관점.
- **다운타임 없는 배포는 사치가 아니라 기본기였습니다.** PM2 reload 한 줄의 무게.
다음 프로젝트에서는 처음부터 부하 테스트를 일정에 넣으려고 합니다.
시연 5분 전에 배우는 건 한 번이면 충분하니까요. 읽어주셔서 감사합니다! 🏃
`,
  qa: `# 런크루 — 면접 예상 질문 & 모범 답안
## Q1. Presigned URL 방식으로 바꿨을 때의 트레이드오프는 무엇인가요?
**모범 답안**: 서버 부하는 사라졌지만 통제권도 일부 넘어갑니다. 서버 경유 시 가능했던
업로드 시점의 파일 검증(용량·형식·악성 파일)을 못 하게 되어, URL 발급 시 Content-Type과
용량 조건을 제한하고 업로드 완료 후 검증 단계를 추가하는 것으로 보완했습니다.
또한 프론트 구현 복잡도가 올라가서 팀원과 업로드 플로우를 함께 재설계해야 했습니다.
성능 개선은 공짜가 아니라 책임의 위치를 옮기는 일이라는 걸 배웠습니다.
## Q2. EC2 안에 MySQL을 함께 띄웠는데, RDS를 쓰지 않은 이유는 무엇인가요?
**모범 답안**: 학생 팀 프로젝트의 예산 제약으로 프리티어 내 운영이 우선이었고,
예상 트래픽 규모에서 성능상 문제가 없다고 판단했습니다. 다만 백업이 수동이고 서버
장애 시 DB가 함께 죽는 단일 장애점이라는 리스크는 인지하고 있었고, 실서비스 전환
시에는 RDS 분리와 자동 백업을 1순위로 두겠다고 답할 수 있습니다. 비용 제약 속 의사결정과
그 한계를 함께 설명하는 것이 포인트입니다.
## Q3. 30초 다운타임을 제거했다고 했는데, 무중단 배포가 정상 동작하는지 어떻게 확인했나요?
**모범 답안**: 배포 중에 1초 간격으로 헬스체크 엔드포인트를 호출하는 스크립트를 돌려
응답 실패가 0건인지 확인했습니다. 또한 PM2 reload가 순차 재시작하는 동안 구버전과
신버전이 공존하는 순간이 있어, DB 스키마 변경이 있는 배포는 하위 호환을 유지하는
마이그레이션(컬럼 추가 → 코드 배포 → 정리) 순서로 진행해야 한다는 것도 이때 알게 됐습니다.
## Q4. 팀에서 백엔드를 혼자 담당했는데, 본인 코드의 품질은 어떻게 관리했나요?
**모범 답안**: 리뷰해줄 백엔드 동료가 없다는 게 가장 큰 리스크였습니다. 그래서 ① 핵심
비즈니스 로직(참석 투표, 출석 집계)에 테스트 코드를 우선 작성해 CI에서 강제하고
② API 명세를 문서로 먼저 작성해 프론트 팀원의 리뷰를 받는 방식으로 보완했습니다.
혼자 담당하는 영역일수록 자동화된 검증과 문서가 리뷰어를 대신한다고 생각합니다.
## Q5. 트래픽이 10배가 되면 현재 구조에서 어디가 먼저 무너질까요?
**모범 답안**: EC2 한 대에 API와 MySQL이 동거하는 구조상 DB가 먼저 병목이 됩니다.
순서로는 ① RDS 분리 ② 조회가 많은 크루 탐색에 인덱스 튜닝과 캐시 도입 ③ API 서버를
무상태로 유지하고 있으므로 로드밸런서 뒤에 수평 확장 — 이 순서로 대응하겠습니다.
막연히 "스케일아웃 하겠다"가 아니라 현재 구조의 약점부터 짚는 것이 좋은 답변입니다.
`,
};

const BACKEND_SET_1: ExampleSet = {
  readme: `# 썸네일리
> 이미지 업로드 한 번으로 3종 해상도 썸네일을 자동 생성·서빙하는 서버리스 파이프라인
## 📌 프로젝트 개요
블로그 서비스를 운영하는 지인이 "이미지 리사이징 서버 비용이 아깝다"고 한 말에서 출발했습니다.
상시 서버 없이 업로드 이벤트가 있을 때만 컴퓨팅이 도는 구조로 만들어,
간헐적 트래픽에서 비용을 최소화하는 것이 목표였습니다.
- **개발 기간**: 2025.10 ~ 2025.12 (8주, 1인 개발)
- **주요 성과**: p99 응답 4.1초 → 1.4초, 월 인프라 비용 EC2 상시 운영 대비 92% 절감
## 🛠 기술 스택
| 분류 | 기술 | 선택 이유 |
| --- | --- | --- |
| Compute | AWS Lambda (Node.js 20, sharp) | 업로드 이벤트 기반 간헐 트래픽에 요청당 과금이 최적 |
| Storage / CDN | S3 (원본·파생 분리 버킷), CloudFront | 파생본 재생성 시 원본 오염 방지 + 엣지 캐싱 |
| IaC | Terraform | dev/prod 환경을 코드로 일치, 리뷰 가능한 인프라 변경 |
| 관측 | CloudWatch Logs Insights | p50/p99 지연·에러율 대시보드 |
## 🏗 아키텍처
\`\`\`
[Client] ─업로드─▶ [S3 원본 버킷]
                        │ ObjectCreated 이벤트
                        ▼
                 [Lambda (sharp 리사이징)] ─▶ [S3 파생 버킷] ─▶ [CloudFront] ─▶ 사용자
                        │
                        └─ 실패 시 ─▶ [DLQ (SQS)] ─▶ 재처리 Lambda
\`\`\`
## ✨ 주요 기능
- 업로드 즉시 3종 해상도(1200/600/300px) 자동 생성, 평균 1.2초 내 완료
- 실패 이벤트 DLQ 격리 후 자동 재처리 (유실 0건)
- Terraform 모듈화로 dev/prod 배포 명령 한 줄 통일
- CloudWatch 지표 기반 p99 지연 알람
## 🔧 트러블슈팅
### Lambda 콜드스타트: p99 4.1초 → 1.4초
**문제**: 배포 후 CloudWatch로 지연 분포를 확인하니 p50은 0.9초인데 p99가 4.1초였습니다.
원인은 sharp 네이티브 바이너리가 포함된 12MB 번들의 콜드스타트 초기화 비용이었습니다.
**해결**: sharp를 Lambda Layer로 분리해 함수 번들을 1.8MB로 줄이고, 메모리를 512MB→1024MB로
올려 CPU 할당을 함께 확보했습니다. 메모리 상향은 비용 증가지만 실행 시간이 절반으로 줄어
요청당 비용은 오히려 감소했습니다.
**결과**: p99 1.4초, 요청당 비용 18% 감소.
### S3 이벤트 중복 실행으로 파생본이 이중 생성되는 문제
**문제**: S3 이벤트는 at-least-once 전달이라 같은 업로드에 Lambda가 두 번 뜨는 경우가 있었고,
간헐적으로 파생본 생성이 충돌해 깨진 이미지가 만들어졌습니다.
**해결**: 파생 키(원본 ETag 기반)를 조건부 쓰기(If-None-Match)로 저장해 멱등성을 확보했습니다.
이벤트 기반 시스템에서는 "중복 실행을 막는 것"이 아니라 "중복 실행이 무해하도록" 설계해야
한다는 것을 배웠습니다.
**결과**: 깨진 이미지 재발 0건.
## 🚀 실행 방법
\`\`\`bash
git clone https://github.com/username/thumbnaily
cd thumbnaily/infra
terraform init && terraform apply -var-file=dev.tfvars
\`\`\`
`,
  blog: `# 서버 없는 이미지 파이프라인으로 월 비용 92%를 줄인 이야기
안녕하세요! 8주 동안 서버리스 이미지 리사이징 파이프라인 "썸네일리"를 만들며 겪은
삽질을 공유합니다. 결론부터: 서버리스는 서버 관리를 없애주지만, 대신 분산 시스템의
고전 문제들(콜드스타트, 중복 이벤트)을 정면으로 만나게 해줍니다.
## EC2 켜두기 vs 이벤트에만 반응하기
하루 업로드가 몇백 건인 서비스에 EC2를 상시로 켜두는 건 주차장에 24시간 택시를
대기시키는 것과 같았습니다. 요청당 과금인 Lambda가 트래픽 패턴과 정확히 맞았고,
실제로 월 비용이 EC2 대비 92% 줄었습니다. 다만 공짜 점심은 없었습니다.
## 첫 번째 벽: p99라는 진실
평균 응답만 보면 0.9초로 훌륭했는데, p99를 뽑아보니 4.1초였습니다. 100명 중 1명은
4초를 기다린다는 뜻이죠. 원인은 sharp 바이너리가 든 12MB 번들의 콜드스타트였습니다.
Layer 분리로 번들을 1.8MB까지 깎고 메모리를 올려 CPU를 확보하니 1.4초가 됐습니다.
"평균은 거짓말을 한다, 분포를 봐라"를 몸으로 배운 순간이었습니다.
## 두 번째 벽: 같은 이벤트가 두 번 온다
어느 날 깨진 썸네일 제보를 받았습니다. 로그를 추적하니 같은 업로드에 Lambda가
두 번 실행돼 쓰기가 충돌한 것. S3 이벤트가 at-least-once라는 문서를 그제야 제대로
읽었습니다. 해결은 중복을 막는 게 아니라 멱등하게 만드는 것 — ETag 기반 조건부
쓰기로 두 번 실행돼도 결과가 같도록 바꿨습니다.
## 배운 것
- **비용 최적화의 시작은 트래픽 패턴 분석입니다.** 패턴이 간헐적이어야 서버리스가 이깁니다.
- **분산 시스템 문서의 "at-least-once"는 경고문입니다.** 반드시 멱등성으로 대비해야 합니다.
- 다음엔 프로비저닝된 동시성으로 p99를 1초 아래로 밀어볼 계획입니다.
인프라는 숫자로 말해야 한다는 걸 배운 8주였습니다. 읽어주셔서 감사합니다! 🙌
`,
  qa: `# 썸네일리 — 면접 예상 질문 & 모범 답안
## Q1. EC2가 아닌 Lambda를 선택한 근거는 무엇인가요?
**모범 답안**: 하루 수백 건 수준의 간헐적 업로드 트래픽을 분석한 결과(상황) 유휴 시간
비용 제거가 핵심 과제였고(과제), 이벤트 기반 Lambda + S3 트리거 구조로 전환해(행동)
월 인프라 비용을 상시 EC2 대비 92% 절감했습니다(결과). 반대로 상시 고트래픽이라면
컨테이너 상시 운영이 유리하다는 트레이드오프도 함께 설명할 수 있습니다.
## Q2. 콜드스타트 문제를 어떻게 진단하고 해결했나요?
**모범 답안**: 평균 지연은 정상인데 사용자 불만이 있어 p99를 확인하니 4.1초였습니다(상황).
CloudWatch Logs Insights로 콜드스타트 구간을 분리 측정해 12MB 번들 초기화가 원인임을
특정했고(행동), Layer 분리와 메모리 상향으로 p99 1.4초를 달성했습니다(결과).
평균이 아닌 분포로 진단하는 습관이 이 문제의 핵심이었습니다.
## Q3. S3 이벤트 중복 실행은 왜 발생하고 어떻게 대응했나요?
**모범 답안**: S3 이벤트는 at-least-once 전달을 보장하므로 중복 실행은 막을 수 없는
전제입니다. 파생 키를 원본 ETag 기반으로 결정하고 조건부 쓰기로 저장해 두 번 실행돼도
결과가 동일하도록 멱등성을 확보했습니다. "중복을 막기"보다 "중복이 무해하게"가
이벤트 기반 설계의 원칙이라고 생각합니다.
## Q4. Terraform을 도입해서 실제로 얻은 것은 무엇인가요?
**모범 답안**: 콘솔 클릭으로 만든 초기 인프라는 dev와 prod가 미묘하게 달라 재현 불가능한
버그를 만들었습니다. Terraform 모듈화 이후 환경 차이가 코드 리뷰로 잡히게 됐고, 신규 환경
구축이 반나절에서 10분으로 줄었습니다. 다만 상태 파일 관리라는 새 운영 부담이 생긴다는
한계도 경험했습니다.
## Q5. 트래픽이 100배가 되면 어디가 먼저 문제가 될까요?
**모범 답안**: Lambda 동시 실행 한도와 파생 버킷의 같은 프리픽스 쓰기 집중이 병목 후보입니다.
동시 실행 한도 상향 요청, 키 프리픽스 분산, 그리고 재처리 큐의 처리량 검증 순서로
대응하겠습니다. 미리 부하 테스트로 한도를 확인해두는 것이 우선이라고 답하겠습니다.
`,
};

const BACKEND_SET_2: ExampleSet = {
  readme: `# 랭킹워크
> Redis Sorted Set으로 정렬 쿼리 병목을 제거한 실시간 걸음 수 순위 API
## 📌 프로젝트 개요
사내 걷기 챌린지 앱의 백엔드로, 참가자 수만 명의 걸음 수 순위를 실시간으로 보여줘야
했습니다. RDB 정렬 쿼리로 시작했다가 피크 시간 병목을 겪고 Redis 기반으로 재설계한
과정이 이 프로젝트의 핵심 경험입니다.
- **개발 기간**: 2025.08 ~ 2025.11 (12주, 2인 팀 / 본인 역할: 백엔드 전담)
- **주요 성과**: 상위 100위 조회 3.2초 → 평균 3ms, 피크 시간 DB CPU 89% → 31%
## 🛠 기술 스택
| 분류 | 기술 | 선택 이유 |
| --- | --- | --- |
| API | Spring Boot 3, JPA | 팀 학습 곡선 없이 생산성 확보, 트랜잭션 관리 |
| 순위 저장소 | Redis (Sorted Set) | O(log N) 점수 갱신과 범위 조회로 실시간 순위에 최적 |
| 영속화 | MySQL 8 | 걸음 수 이력의 원본(source of truth) 보관 |
| 부하 테스트 | k6 | 재설계 전후 성능을 같은 시나리오로 비교 검증 |
## 🏗 아키텍처
\`\`\`
[App Client] ─▶ [Spring Boot API]
                    │ 점수 갱신: ZADD (즉시)
                    ├────────────▶ [Redis ZSET] ◀─ 순위 조회: ZREVRANGE
                    │ 이력 기록: 비동기 배치
                    └────────────▶ [MySQL (원본 이력)]
                                        │ Redis 장애 시
                                        └─ 복원 스크립트 ─▶ ZSET 재구성
\`\`\`
## ✨ 주요 기능
- 상위 100위 + 내 순위 조회 평균 3ms (k6 부하 테스트, 동시 500명 기준)
- 점수 갱신(동기)과 이력 영속화(비동기)의 책임 분리
- Redis 장애 대비: MySQL 이력에서 ZSET을 재구성하는 복원 스크립트
- 챌린지 기간별 순위판 분리 (ZSET 키 네임스페이스 설계)
## 🔧 트러블슈팅
### 정렬 쿼리 병목: 조회 3.2초 → 3ms
**문제**: 참가자 5만 명 시점부터 피크 시간(점심 직후) 순위 조회가 3.2초까지 치솟았습니다.
ORDER BY 인덱스를 태워도 걸음 수 갱신이 몰리는 시간대엔 갱신 부하와 조회 부하가
같은 테이블에서 경합했습니다.
**해결**: 순위 로직을 Redis Sorted Set으로 이전하고 MySQL은 이력 원본 역할로 한정했습니다.
갱신은 ZADD, 조회는 ZREVRANGE로 모두 밀리초 단위가 됐고, k6로 재설계 전후를 동일
시나리오 비교해 수치를 검증했습니다.
**결과**: 조회 평균 3ms, 피크 시간 DB CPU 89% → 31%.
### Redis 재기동 시 순위가 통째로 사라지는 문제
**문제**: 운영 중 Redis 인스턴스 교체가 필요해졌는데, ZSET이 날아가면 순위판이 초기화되는
구조적 리스크를 뒤늦게 인지했습니다.
**해결**: Redis를 캐시가 아닌 "파생 저장소"로 재정의하고, 원본인 MySQL 이력에서 ZSET을
재구성하는 복원 스크립트를 만들어 재기동 절차에 포함했습니다. 5만 명 기준 복원 42초로
공지 없이 교체 가능한 수준을 확인했습니다.
**결과**: 인스턴스 교체 2회 무사고 진행.
## 🚀 실행 방법
\`\`\`bash
git clone https://github.com/username/rankwalk
cd rankwalk
docker compose up -d   # MySQL + Redis
./gradlew bootRun
\`\`\`
`,
  blog: `# 순위 조회 3.2초를 3ms로 — RDB를 탓하기 전에 배운 것들
안녕하세요! 사내 걷기 챌린지 백엔드를 만들며 "ORDER BY가 왜 느려지는가"를 몸으로
배운 12주를 공유합니다. 스포일러: RDB가 나쁜 게 아니라 제 설계가 순진했습니다.
## 5만 명이 만든 3.2초
참가자 5만을 넘긴 어느 점심시간, 순위판이 3.2초씩 걸린다는 제보가 왔습니다.
인덱스는 있었어요. 문제는 점심 직후 걸음 수 갱신이 폭주하는 시간에 조회까지 같은
테이블로 몰린다는 것. 갱신과 조회가 서로 발목을 잡는 구조였습니다.
## "정석"이라 불리는 데는 이유가 있다
순위 = Redis Sorted Set이라는 조합은 너무 유명해서 오히려 의심했는데, 써보니
유명한 이유가 있었습니다. ZADD로 갱신, ZREVRANGE로 조회 — 둘 다 밀리초입니다.
다만 그대로 옮기면 안 되는 게, Redis가 죽으면 순위가 증발합니다. 그래서 MySQL을
원본으로 남기고 Redis는 "언제든 재구성 가능한 파생 저장소"로 역할을 정의했습니다.
이 역할 정의 덕분에 나중에 인스턴스 교체를 무사고로 두 번 했습니다.
## 숫자로 검증하는 습관
이번에 처음으로 k6 부하 테스트를 재설계 전후에 같은 시나리오로 돌렸습니다.
"빨라진 것 같다"가 아니라 "동시 500명에서 3.2초 → 3ms, DB CPU 89% → 31%"라고
말할 수 있게 된 것이 이 프로젝트에서 가장 큰 수확이었습니다.
## 배운 것
- **병목의 원인은 쿼리가 아니라 부하의 충돌이었습니다.** 진단이 해결보다 어렵습니다.
- **저장소마다 역할을 문장으로 정의하세요.** "Redis는 파생, MySQL은 원본" 한 줄이 장애 대응을 설계해줍니다.
- 다음엔 Redis Cluster 샤딩으로 참가자 50만 시나리오를 실험해볼 생각입니다.
읽어주셔서 감사합니다. 여러분의 ORDER BY는 안녕하신가요? 🙌
`,
  qa: `# 랭킹워크 — 면접 예상 질문 & 모범 답안
## Q1. Redis 도입을 결정하기까지의 과정을 설명해주세요.
**모범 답안**: 참가자 5만 명 시점에 피크 시간 순위 조회가 3.2초로 치솟았고(상황),
갱신·조회 부하가 같은 테이블에서 경합하는 게 원인이었습니다(과제 진단). 인덱스 튜닝을
먼저 시도한 뒤 한계를 확인하고 Sorted Set으로 순위 로직을 이전했으며(행동), 조회 3ms·
DB CPU 89%→31%를 k6 부하 테스트로 검증했습니다(결과). 바로 신기술로 점프하지 않고
기존 스택의 한계를 먼저 확인한 순서가 포인트입니다.
## Q2. Redis가 장애 나면 순위 데이터는 어떻게 되나요?
**모범 답안**: Redis를 캐시가 아닌 파생 저장소로 정의하고 원본은 MySQL 이력에 두었습니다.
장애·교체 시 이력에서 ZSET을 재구성하는 복원 스크립트를 운영 절차에 포함했고, 5만 명
기준 42초 복원을 실측했습니다. 실제 인스턴스 교체를 2회 무사고로 진행하며 검증됐습니다.
## Q3. 점수 갱신과 이력 저장을 왜 분리했고, 정합성은 어떻게 지키나요?
**모범 답안**: 순위 반영은 실시간성이 중요하고 이력은 유실만 없으면 되는, 요구사항이 다른
두 작업이라 동기 ZADD + 비동기 배치 영속화로 분리했습니다. 배치 실패 시 재시도 큐로
보존해 최종 정합성을 확보하고, 일 배치로 ZSET과 이력 합계를 대사(reconcile)해 드리프트를
감지합니다. 강한 정합성 대신 최종 정합성을 선택한 근거를 설명할 수 있습니다.
## Q4. 2인 팀에서 백엔드를 전담했는데 협업은 어떻게 했나요?
**모범 답안**: API 명세를 코드보다 먼저 문서로 작성해 프론트 담당과 리뷰했고, 순위 정책
(동점 처리, 집계 기준 시각) 같은 도메인 규칙을 문서에 못 박은 것이 재작업을 크게 줄였습니다.
전담 영역일수록 결정 사항을 문서로 남겨 상대가 검증할 수 있게 하는 것이 협업이라고
생각합니다.
## Q5. 참가자가 50만 명이 되면 무엇을 바꾸시겠습니까?
**모범 답안**: 단일 Redis의 메모리와 처리량이 먼저 한계에 옵니다. ① ZSET 키를 챌린지·리그
단위로 샤딩하고 ② 전체 순위는 주기 집계로 전환(실시간은 리그 내 순위만) ③ 복원 스크립트를
샤드 병렬로 개선 — 이 순서로 대응하겠습니다. "전부 실시간"이라는 요구부터 재협상하는 것이
현실적인 첫 수라고 답하겠습니다.
`,
};

const AI_SET_1: ExampleSet = {
  readme: `# 미닛츠
> 회의 녹취록을 올리면 결정사항·액션아이템을 구조화해주는 LLM 요약 서비스
## 📌 프로젝트 개요
"회의는 했는데 누가 뭘 하기로 했더라?"를 없애는 것이 목표였습니다. 긴 녹취 텍스트에서
결정사항, 액션아이템(담당자·기한 포함)을 JSON으로 구조화 추출하고, 품질을 감으로 판단하지
않도록 골든셋 평가 파이프라인을 함께 구축했습니다.
- **개발 기간**: 2026.01 ~ 2026.02 (6주, 1인 개발)
- **주요 성과**: 액션아이템 추출 재현율 0.62 → 0.91 (골든셋 30건 기준)
## 🛠 기술 스택
| 분류 | 기술 | 선택 이유 |
| --- | --- | --- |
| API | FastAPI | 비동기 스트리밍 응답과 pydantic 검증의 자연스러운 결합 |
| LLM | Claude API (structured output) | 긴 컨텍스트 처리와 스키마 강제 출력 |
| 평가 | pytest + 골든셋 30건 | 프롬프트 변경의 회귀를 CI에서 자동 감지 |
| 프론트 | Streamlit | 평가 결과·추출 결과를 빠르게 시각화 |
## 🏗 아키텍처
\`\`\`
[녹취 업로드] ─▶ [FastAPI]
                   │ 청크 분할 (문맥 overlap)
                   ▼
             [Claude API (structured output)] ─▶ [청크별 결과 병합·중복 제거]
                   │                                    │
                   ▼                                    ▼
             [골든셋 평가 (CI)]                  [JSON: 결정사항/액션아이템]
\`\`\`
## ✨ 주요 기능
- 결정사항 / 액션아이템(담당자·기한) JSON 구조화 추출
- 1시간 이상 녹취도 청크 분할 + 병합으로 누락 없이 처리
- 프롬프트 변경 시 골든셋 30건 자동 평가 → 재현율 하락하면 CI 실패
- 원문에 없는 담당자 이름 검출 시 후처리 필터링 (환각 방어)
## 🔧 트러블슈팅
### 장문 회의록 후반부 액션아이템 누락: 재현율 0.62 → 0.91
**문제**: 데모용 짧은 회의록에선 완벽했는데, 실제 1시간짜리 녹취를 넣으니 후반부
액션아이템을 반복적으로 놓쳤습니다. 더 심각한 문제는 제가 그 품질을 "느낌"으로만
판단하고 있었다는 점입니다.
**해결**: 먼저 회의록 30건에 정답 라벨을 달아 골든셋을 만들고 재현율을 측정 가능하게
했습니다(측정이 먼저). 그다음 문맥 overlap을 둔 청크 분할 + 청크별 추출 + 병합·중복 제거
전략을 도입하고, 골든셋 수치로 각 변경의 효과를 검증했습니다.
**결과**: 재현율 0.62 → 0.91. 이후 모든 프롬프트 변경이 CI에서 자동 검증됩니다.
### 원문에 없는 담당자 이름이 생성되는 환각
**문제**: 참석자가 아닌 사람이 액션아이템 담당자로 배정되는 사례가 골든셋 평가에서
발견됐습니다.
**해결**: 구조화 출력으로 자유 서술을 차단한 뒤에도 남는 환각이라, 추출된 담당자명을
원문 대조로 검증하는 후처리 필터를 추가하고 미검증 이름은 "미지정"으로 강등했습니다.
**결과**: 골든셋 기준 담당자 환각 0건.
## 🚀 실행 방법
\`\`\`bash
git clone https://github.com/username/minutes
cd minutes
pip install -r requirements.txt
cp .env.example .env   # ANTHROPIC_API_KEY 설정
uvicorn app.main:app --reload
\`\`\`
`,
  blog: `# LLM 서비스는 프롬프트보다 평가가 본체더라고요
안녕하세요! 회의록 요약 봇 "미닛츠"를 6주간 만들며 얻은 결론을 먼저 말씀드리면 —
LLM 기능 개발의 8할은 프롬프트 작성이 아니라 "잘 되는지 측정하는 체계"였습니다.
## 데모의 함정
개발 초기, 10분짜리 회의록으로 시연하면 결과가 완벽했습니다. 뿌듯했죠.
그런데 실제 1시간짜리 녹취를 넣자 후반부 액션아이템이 뭉텅이로 사라졌습니다.
더 부끄러운 건, 그게 "얼마나" 사라지는지 저도 몰랐다는 겁니다. 측정을 안 했으니까요.
## 골든셋 30건이 바꾼 것
회의록 30건에 정답 라벨을 손으로 달았습니다. 지루한 이틀이었지만, 이때부터 개발이
게임처럼 바뀌었습니다. 재현율 0.62라는 기준선이 생겼고, 청크 분할을 넣으니 0.78,
overlap을 조정하니 0.85, 병합 중복 제거를 다듬으니 0.91. 모든 변경이 숫자로 평가되니
"이게 나아진 건가?"라는 불안이 사라졌습니다. 지금은 프롬프트를 고치면 CI가 골든셋을
자동으로 돌려서, 재현율이 떨어지는 변경은 머지 자체가 안 됩니다.
## 환각과의 싸움은 후처리에서
구조화 출력으로 형식은 잡아도, 참석하지도 않은 사람이 담당자로 배정되는 환각은
남았습니다. 해결은 의외로 소박했습니다. 추출된 이름을 원문과 대조해서 없으면
"미지정"으로 강등하는 후처리 한 단계. LLM을 믿되 검증은 코드로 하는 것이
실서비스의 자세라고 생각하게 됐습니다.
## 배운 것
- **평가셋 없는 LLM 개발은 나침반 없는 항해입니다.** 라벨링 이틀이 6주를 살렸습니다.
- **환각 방어의 마지막 층은 프롬프트가 아니라 후처리 코드입니다.**
- 다음 글에선 평가 파이프라인을 CI에 붙인 구성을 자세히 다뤄보겠습니다.
LLM 다루시는 분들, 골든셋부터 만드세요. 진심입니다. 읽어주셔서 감사합니다! 🙌
`,
  qa: `# 미닛츠 — 면접 예상 질문 & 모범 답안
## Q1. LLM 출력 품질을 어떻게 보장했나요?
**모범 답안**: 데모와 실사용 품질의 괴리를 발견했지만 그 크기를 측정할 수단이 없었습니다
(상황). 정량 평가 체계 구축을 최우선 과제로 정하고(과제) 회의록 30건에 정답 라벨을 달아
골든셋을 만든 뒤, 모든 프롬프트·파이프라인 변경을 재현율로 검증하고 CI에 연결했습니다
(행동). 재현율 0.62→0.91을 달성했고 이후 회귀는 머지 단계에서 자동 차단됩니다(결과).
## Q2. 장문 입력에서 후반부 누락은 왜 생기고 어떻게 해결했나요?
**모범 답안**: 긴 컨텍스트에서 후반부 정보의 추출 충실도가 떨어지는 현상이었습니다.
문맥 overlap을 둔 청크 분할로 각 구간을 독립 추출한 뒤 병합·중복 제거하는 전략으로
전환했고, overlap 크기 같은 하이퍼파라미터는 감이 아니라 골든셋 수치로 결정했습니다.
## Q3. 환각(hallucination)은 어떻게 다뤘나요?
**모범 답안**: 두 층으로 방어했습니다. 1차는 구조화 출력 스키마로 자유 서술을 차단,
2차는 추출된 담당자명을 원문 대조로 검증해 없는 이름은 "미지정"으로 강등하는 후처리입니다.
평가셋에서 담당자 환각 사례를 발견했기에 만들 수 있었던 방어층이라, 평가와 방어가
한 몸이라고 생각합니다.
## Q4. LLM API 비용은 어떻게 관리했나요?
**모범 답안**: 시스템 프롬프트와 few-shot 예시처럼 고정된 부분에 프롬프트 캐싱을 적용해
반복 호출의 입력 비용을 줄였고, 골든셋 평가는 배치로 몰아 실행해 개발 중 불필요한 호출을
억제했습니다. 기능 단위가 아니라 "요청당 원가"로 비용을 보는 습관을 얻었습니다.
## Q5. 이 프로젝트를 다시 만든다면 무엇을 바꾸시겠습니까?
**모범 답안**: 골든셋을 프로젝트 첫 주에 만들겠습니다. 이번엔 4주 차에 만들어서 그 전의
프롬프트 시행착오가 전부 감에 의존했는데, 처음부터 있었다면 절반의 시간으로 같은 품질에
도달했을 겁니다. LLM 프로젝트의 1순위 산출물은 코드가 아니라 평가셋이라는 게 가장 큰
교훈입니다.
`,
};

const AI_SET_2: ExampleSet = {
  readme: `# 시세어때
> 중고 전자기기 거래 데이터를 수집·학습해 적정 시세와 신뢰 구간을 제시하는 예측 대시보드
## 📌 프로젝트 개요
중고 거래에서 "이 가격이 적정한가?"를 데이터로 답하고 싶었습니다. 거래 플랫폼의 시세
데이터를 매일 수집하고, LightGBM으로 모델을 학습해 기기 조건별 예측가와 신뢰 구간을
대시보드로 제공합니다. 모델링보다 데이터 품질과의 싸움이 본체였던 프로젝트입니다.
- **개발 기간**: 2025.12 ~ 2026.02 (10주, 1인 개발)
- **주요 성과**: 검증셋 MAE 40% 개선 (노이즈 필터링 + 도메인 피처), 주간 자동 재학습
## 🛠 기술 스택
| 분류 | 기술 | 선택 이유 |
| --- | --- | --- |
| 수집/전처리 | Python, pandas | 이상치 처리·피처 엔지니어링의 표준 도구 |
| 모델 | LightGBM | 테이블 데이터에 강하고 학습이 빨라 실험 반복에 유리 |
| 서빙 | Streamlit | 예측 결과·신뢰 구간을 빠르게 시각화 |
| 파이프라인 | cron + 학습 스크립트 | 주간 재학습을 단순한 구조로 자동화 |
## 🏗 아키텍처
\`\`\`
[수집 크롤러 (일 1회)] ─▶ [원시 데이터 저장]
                              │ 노이즈 필터 (통계 + 도메인 규칙)
                              ▼
                        [전처리·피처 파이프라인]
                              │ 주 1회
                              ▼
                        [LightGBM 학습·검증] ─MAE 리포트─▶ [모델 레지스트리(버전 관리)]
                              │
                              ▼
                        [Streamlit 대시보드: 예측가 + 신뢰 구간]
\`\`\`
## ✨ 주요 기능
- 기기 모델·상태·연식 입력 시 예측 시세와 신뢰 구간 표시
- 통계(IQR) + 도메인 규칙("부품", "급처" 키워드) 결합 노이즈 필터
- 주간 자동 재학습 + 학습마다 MAE 리포트 저장 (성능 이력 추적)
- 모델 버전 관리로 성능 저하 시 이전 버전 롤백 가능
## 🔧 트러블슈팅
### 노이즈 데이터로 인한 예측 왜곡: 검증셋 MAE 40% 개선
**문제**: 수집 데이터를 그대로 학습하니 예측가가 체감보다 20~30% 낮았습니다. 원인을
추적하니 "아이폰 14 급처 5만원" 같은 낚시 글, 부품 판매, 오타 가격이 학습 데이터의
약 30%를 오염시키고 있었습니다.
**해결**: IQR 기반 이상치 제거만으로는 정상적인 저가 거래까지 잘려나가서, 제목 키워드
도메인 규칙("부품", "급처", "교신", "매입")을 결합한 2단 필터를 만들었습니다. 필터의
과잉 제거 여부는 샘플 100건을 수동 검수해 확인했습니다.
**결과**: 검증셋 MAE 40% 개선, 예측가의 체감 정확도 확보.
### 하이퍼파라미터 튜닝보다 피처 하나가 컸던 경험
**문제**: LightGBM 튜닝에 일주일을 쏟았지만 MAE 개선이 3%에 그쳤습니다.
**해결**: 방향을 바꿔 도메인 지식 기반 피처를 실험했습니다. "출시 후 경과 개월" 피처
하나를 추가하자 MAE가 11% 더 떨어졌습니다. 이후 실험 우선순위를 "튜닝보다 피처,
피처보다 데이터 품질"로 재정렬했습니다.
**결과**: 같은 노력 대비 개선 폭 기준으로 실험 우선순위 체계 확립.
## 🚀 실행 방법
\`\`\`bash
git clone https://github.com/username/how-much-used
cd how-much-used
pip install -r requirements.txt
python pipeline/train.py   # 학습 + MAE 리포트
streamlit run app.py
\`\`\`
`,
  blog: `# 데이터 프로젝트의 8할은 전처리라는 말, 진짜였습니다
안녕하세요! 중고 시세 예측 대시보드 "시세어때"를 10주간 만든 기록입니다.
모델링을 배우고 싶어 시작했는데, 정작 10주 중 6주를 데이터 청소에 썼습니다.
그리고 그게 맞는 배분이었다는 걸 나중에 알았습니다.
## "아이폰 14 급처 5만원"의 함정
첫 학습 모델의 예측가가 체감보다 20~30% 낮게 나왔습니다. 데이터를 뜯어보니 범인은
낚시 글, 부품 판매, 오타 가격. 수집 데이터의 30%가 노이즈였습니다.
IQR로 이상치를 자르면 되겠지 했는데, 이러면 진짜 급매 거래까지 같이 잘립니다.
결국 통계 필터에 도메인 규칙("부품", "급처" 키워드)을 얹은 2단 필터를 만들고,
샘플 100건을 눈으로 검수해서 과잉 제거가 없는지 확인했습니다. 지루하지만 이 검수가
모델 성능의 바닥을 만들었습니다.
## 튜닝 일주일 vs 피처 하나
하이퍼파라미터 튜닝에 일주일을 갈아 넣고 얻은 개선이 3%였습니다. 허탈하던 차에
"출시 후 경과 개월" 피처를 추가했더니 11%가 떨어졌습니다. 기계를 쥐어짜는 것보다
도메인을 이해하는 게 압도적으로 효율적이라는 것, 데이터 과학 책들이 왜 그렇게
피처 엔지니어링을 강조하는지 체감한 순간이었습니다.
## 일회성 분석을 파이프라인으로
노트북(ipynb)에서 시작한 코드를 주간 재학습 파이프라인으로 옮기면서, 학습마다 MAE
리포트를 남기고 모델을 버전 관리했습니다. 덕분에 어느 주 성능이 떨어졌을 때 원인
(수집 소스의 구조 변경)을 리포트 이력으로 특정하고 이전 버전으로 롤백할 수 있었습니다.
## 배운 것
- **모델 성능의 상한은 데이터 품질이 정합니다.** 튜닝은 그 상한 안에서의 미세 조정입니다.
- **필터는 만들었다고 끝이 아니라 검수해야 합니다.** 과잉 제거는 조용히 성능을 갉아먹습니다.
- 다음엔 Airflow로 파이프라인을 옮기고 피처 스토어를 실험해볼 계획입니다.
데이터로 일하고 싶은 분들께: 화려한 모델보다 지루한 검수가 실력입니다. 감사합니다! 🙌
`,
  qa: `# 시세어때 — 면접 예상 질문 & 모범 답안
## Q1. 데이터 품질 문제를 어떻게 진단하고 해결했나요?
**모범 답안**: 첫 모델의 예측가가 체감 대비 20~30% 낮아 원인을 추적했더니 수집 데이터의
30%가 낚시 글·부품 판매 같은 노이즈였습니다(상황/진단). 신뢰 가능한 학습셋 구축을 과제로
(과제) IQR 통계 필터에 도메인 키워드 규칙을 결합한 2단 필터를 만들고, 샘플 100건 수동
검수로 과잉 제거를 검증했습니다(행동). 검증셋 MAE 40% 개선을 달성했습니다(결과).
## Q2. 모델 성능 개선에서 가장 효과적이었던 것은 무엇인가요?
**모범 답안**: 하이퍼파라미터 튜닝 일주일의 개선이 3%였는데 "출시 후 경과 개월" 피처
하나가 11%를 개선했습니다. 이후 실험 우선순위를 "데이터 품질 > 피처 > 튜닝"으로
재정렬했고, 개선 폭을 노력 대비 수치로 기록해 우선순위를 정하는 습관을 얻었습니다.
## Q3. 예측값을 단일 숫자가 아니라 신뢰 구간으로 제공한 이유는?
**모범 답안**: 중고 시세는 상태·구성품 같은 미관측 변수의 분산이 커서 점 추정만 주면
사용자를 오도할 수 있습니다. quantile 예측으로 구간을 제공해 "이 범위면 적정"이라는
의사결정 형태에 맞췄습니다. 모델 출력 설계는 정확도만이 아니라 사용자의 결정 방식에서
역산해야 한다고 생각합니다.
## Q4. 재학습 파이프라인에서 모델이 나빠지면 어떻게 감지하나요?
**모범 답안**: 학습마다 고정 검증셋 MAE 리포트를 저장해 이력으로 추적하고, 직전 버전보다
임계치 이상 나빠지면 배포하지 않고 이전 버전을 유지합니다. 실제로 수집 소스 구조 변경으로
성능이 급락한 주에 리포트 이력으로 원인을 특정하고 롤백한 경험이 있습니다.
## Q5. 이 프로젝트를 다시 만든다면 무엇을 바꾸시겠습니까?
**모범 답안**: 수집 단계부터 데이터 검증(스키마·범위 체크)을 넣겠습니다. 이번엔 학습 직전
전처리에서 노이즈를 걸렀는데, 수집 시점에 걸렀다면 원인 추적이 훨씬 빨랐을 겁니다.
"쓰레기 입력은 최대한 상류에서 막는다"가 데이터 파이프라인에서 얻은 가장 큰 교훈입니다.
`,
};

/* ─────────────────────────────────────────────
   유형별 예시 상수 — 이 객체만 교체하면 됨
───────────────────────────────────────────── */

export const EXAMPLES: Record<ProjectType, ProjectTypeExamples> = {
  web: {
    label: "웹서비스",
    sets: [WEB_SET_1, WEB_SET_2],
  },
  "backend-infra": {
    label: "백엔드·인프라",
    sets: [BACKEND_SET_1, BACKEND_SET_2],
  },
  "ai-data": {
    label: "AI·데이터",
    sets: [AI_SET_1, AI_SET_2],
  },
};
